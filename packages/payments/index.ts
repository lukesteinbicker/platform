import "server-only";

// Polar functionality has been removed for simplicity
// The package is kept for future use
// To re-enable, configure POLAR_ACCESS_TOKEN and integrate in @repo/auth/server

export type { Polar } from "@polar-sh/sdk";
